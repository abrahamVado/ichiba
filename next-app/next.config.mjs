//1.- Exportar la configuración básica de Next.js usando ES Modules.
const nextConfig = {
  reactStrictMode: true
};

//2.- Compartir la configuración para que la CLI de Next pueda consumirla.
export default nextConfig;
